<h1> edit<%= @post.title%> </h1>

<%# form_with(model: @course, local: true) do |f| %> 
<%= form_for @post do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>
 
  <%= f.label :body %>
  <%= f.text_area :body %>
 
  <%= f.label :author %>
  <%= f.text_field :author %>

  <%= f.submit %>
<% end %>
